<%= bootstrap_form_for @slot do |f| %>
  <% if @slot.errors[:base].length > 0 %>
    <div class="alert alert-danger">
      <p>Please fix the errors below.</p>
      <%= @errors.inspect %>
      <ul class="rails-bootstrap-forms-error-summary">
        <% @slot.errors[:base].each do |error| %>
          <li><%= error.message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.text_field :name %>
  <%= f.text_field :description %>
  <div style="text-align: right;">
    <%= link_to 'Cancel', slots_path, class: "btn" %>
    <%= f.primary "Save", id: "submit" %>
  </div>
<% end %>
